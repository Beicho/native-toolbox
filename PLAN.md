# Astro Kit 改造方案 v2

> 目标:从「200 个工具的盒子」变成「一个懂你的随身助手」。
> 不做 AI Agent 对话框 —— 那个赛道拥挤且不是我们的优势。
> 我们做的是**沉默的预测大脑**:不说话,只是让你要用的东西自己浮上来。

---

## Part 0. 底栏液态玻璃:真正的病因

前几次我只看代码结构就说「没问题」,那是敷衍。这次我把 `backdrop-2.0.0.aar` 拆出来看了字节码,确认了三个真实缺陷。

### 缺陷 A:API < 33 的机器上,选中态直接消失(致命)

底栏用的是「三层障眼法」:

| 层 | 作用 |
|---|---|
| 第 1 层 | 玻璃面板 + **灰色**图标 |
| 第 2 层 | `alpha(0f)` 全透明的**强调色**图标(屏幕上看不见,只注册成 backdrop) |
| 第 3 层 | 玻璃滑块,组合第 2 层 —— 滑块经过谁,谁就"透"成强调色 |

这个效果**完全依赖 `lens()` 折射**。而字节码里 `PlatformKt` 暴露了两个独立开关:

```java
public static final boolean isRenderEffectSupported();   // API 31+
public static final boolean isRuntimeShaderSupported();  // API 33+  ← lens 真正需要这个
```

代码里只判断了 `isRenderEffectSupported()`。结果:**Android 12/12L(API 31-32)以及所有 API < 31 的机器上,`lens` 是空操作**,滑块不折射,第 2 层永远看不见 —— 用户完全分不出当前选中的是哪个 Tab,只剩一块 alpha 0.1 的灰影。

这就是你一直觉得"底栏有问题"的根因。它在新机器上好看,在老机器上是坏的,而且坏得很安静。

### 缺陷 B:`vibrancy()` 用错了地方

拆包后 effects 包只有四个文件:`BlurKt` / `LensKt` / `ColorFilterKt` / `RenderEffectKt`。`vibrancy()` 属于 **ColorFilterKt**,是个颜色滤镜,不是玻璃质感。代码里把它放在 `blur` 前面当"玻璃打底",实际是在给背景做饱和度提升,和 Apple 的 vibrancy 语义不是一回事,叠在 blur 前还会让深色模式下的底栏发灰。

### 缺陷 C:滑块吞掉了当前 Tab 的点击

第 3 层滑块盖在第 1 层之上,并且挂了 `detectDragGestures`。滑块正好覆盖当前选中的那一格,所以**点击当前 Tab 没有任何反应**(想回到列表顶部、想刷新,都点不动)。而且拖拽手势在 2 个 Tab 的场景下毫无价值 —— 谁会去拖一个只有两格的开关。

### 结论与修法

253 行代码,为两个 Tab 服务,换来一个在半数设备上残废的效果。**重写,砍到 ~70 行:**

- 保留玻璃面板(`blur` 在 API 31+ 生效,以下自动退化为半透明,这个降级是可靠的)
- **选中态改成直接给图标上色**(`tint = if (selected) accent else secondaryLabel`)—— 不依赖任何 shader,所有机器都对
- 滑块保留,但只做一个跟随移动的高亮胶囊(纯色 + 微透明),不再承担"显色"职责
- 删掉第 2 层和拖拽手势
- `lens` 只在 `isRuntimeShaderSupported()` 为真时叠加,当锦上添花

**效果**:新机器上依然有折射玻璃质感,老机器上是干净的 iOS 风格胶囊。两边都对。

---

## Part 1. 预测大脑(核心差异化)

### 设计原则

- **纯本地**,零网络,零 LLM。省电、隐私、离线可用,这本身就是卖点。
- **不问用户,只观察**。没有"请选择你的偏好"这种问卷。
- **可解释**。卡片上写清楚「你常在周一早上用这个」,不做黑盒。
- **可纠正**。长按「不用推荐这个」,立刻降权。

### 1.1 采集什么(UsageEvent)

每次打开工具,静默记一条(SQLite,单表):

| 字段 | 说明 |
|---|---|
| `route` | 工具路由 |
| `ts` | 时间戳 |
| `hourBucket` | 0-23 |
| `dayOfWeek` | 1-7 |
| `isWeekend` | 布尔 |
| `prevRoute` | 上一个用的工具(算连带) |
| `charging` | 是否在充电 |
| `network` | wifi / cellular / none |
| `dwellMs` | 停留时长(<3 秒视为误触,不计权重) |

**不采集**:位置、通讯录、任何可标识个人的内容。这条要写进隐私说明,是加分项。

### 1.2 怎么算分(ToolScorer)

每个工具的即时得分,四项加权求和 —— 全是查表和乘法,一次计算 200 个工具耗时 < 5ms:

```
score(tool) = w1·近期热度 + w2·时段亲和 + w3·连带概率 + w4·情境加成 − 惩罚
```

**① 近期热度**(指数衰减,半衰期 14 天)
```
Σ exp(-ln2 · (now - ts) / 14天)
```
让上个月的老习惯自然淡出,不用手动清理。

**② 时段亲和**
统计该工具在「当前小时 ±1」和「当前星期几」的历史命中率。早上 7 点打开过 12 次天气,那早上 7 点天气就该排前面。

**③ 连带概率**
一阶马尔可夫转移:用完 A 之后用 B 的频率。比如「拍照 → OCR → 翻译」是常见链路,用完 OCR 就该把翻译顶上来。

**④ 情境加成**(硬规则,不学习)
- 倒数日 ≤ 3 天 → 该倒数日卡片强制置顶
- 有未完成待办且现在是工作日晚上 → 待办卡片加分
- 电量 < 20% → 手电筒、省电相关降权(费电的工具往后放)
- 无网络 → 所有联网工具降权(避免推荐一个点开就报错的东西)

**⑤ 惩罚项**
- 用户长按标记过「不感兴趣」→ 乘 0.2,持续 30 天
- 打开后 < 3 秒就退出 → 该次不计正向权重(说明是误触或没找到想要的)

### 1.3 冷启动(前 7 天没数据怎么办)

内置一张**通用先验表**,按时段给默认推荐,数据攒够后自动让位:

| 时段 | 默认推荐 |
|---|---|
| 6:00-9:00 | 天气、倒数日、历史上的今天 |
| 11:00-13:00 | 记账、计算器、扫码 |
| 14:00-18:00 | 翻译、OCR、PDF、文件传输 |
| 19:00-22:00 | 待办、记账、健康记录 |
| 22:00-次日 | 白噪音、屏幕时间、番茄钟 |

融合公式:`最终分 = α·先验 + (1-α)·个人分`,α 从 1.0 线性衰减到 0(30 天内完成过渡)。用户越用,越不像"通用 App",越像"他自己的 App"。

### 1.4 呈现方式(关键:克制)

**只在主页顶部露出 2-4 个**,叫「**此刻**」。不是一个新页面,不是一个抽屉,就是主页最上面一行。

```
┌──────────────────────────────┐
│  早上好                       │
│  此刻                         │
│  ┌────────┐ ┌────────┐       │
│  │ ☀️ 天气 │ │ 📝 记账 │       │
│  │ 你常在  │ │ 昨天这 │       │
│  │ 早上看  │ │ 会儿记 │       │
│  └────────┘ └────────┘       │
└──────────────────────────────┘
```

每个卡片下面一行小字说明**为什么推荐它**。这个透明度是信任的来源,也是和"玄学推荐"的分界线。

---

## Part 2. 主页重构:从工具网格到生活看板

### 2.1 新的信息层级

```
┌──────────────────────────────┐
│  早上好,今天周五               │  ← 问候(时段自适应)
│                              │
│  【此刻】                     │  ← 预测大脑输出,2-4 个
│  [天气] [记账]                │
│                              │
│  【今天】                     │  ← 活数据卡片,横向滑动
│  ┌──────────────────────┐    │
│  │ 距离春节 18 天         │    │  ← 倒数日:大字 + 可点进详情
│  └──────────────────────┘    │
│  ┌──────────────────────┐    │
│  │ 待办 2/5              │    │  ← 待办:直接在卡上打勾
│  │ ☑ 买菜  □ 写周报      │    │
│  └──────────────────────┘    │
│  ┌──────────────────────┐    │
│  │ 本月支出 ¥2,340       │    │  ← 记账:卡上直接记一笔
│  │ [+ 快速记账]          │    │
│  └──────────────────────┘    │
│                              │
│  🔍 搜索工具                  │
│                              │
│  【常用】                     │  ← 高频 4 个
│  【全部工具】                  │  ← 13 分类折叠,默认收起
└──────────────────────────────┘
```

### 2.2 卡片要能「就地操作」

这是"勤打开"的关键 —— **不用点进工具就能完成动作**:

| 卡片 | 就地能做什么 |
|---|---|
| 待办 | 点圆圈直接打勾,不跳转 |
| 记账 | 卡上有输入框,输金额选分类直接存 |
| 倒数日 | 点一下切换到下一个倒数日 |
| 习惯打卡 | 点一下 +1(喝水 5/8 → 6/8) |
| 天气 | 点一下展开未来 3 天 |

用户每天打开 3 次,每次 10 秒,打个勾记笔账就走 —— 这才是日活。

### 2.3 卡片可配置

设置页里能开关每类卡片、拖拽排序。不喜欢记账的人可以把它关掉,主页就干净了。

### 2.4 数据刷新

现在的 `LaunchedEffect(Unit)` 只跑一次,从工具页返回不刷新。改成监听 `navController` 回到 home 时重新读一次 —— 20 行代码的事。

---

## Part 3. 功能补完(你点名的天气,以及同类问题)

### 3.1 天气:后端已经返回的数据,前端只解析了三成

后端 Open-Meteo 返回了完整的 daily 数组,前端只画了实况温湿度。**已补上**(7 天预报 / 日出日落 / 紫外线),但还欠:
- 24 小时逐时温度曲线(数据已有)
- 体感温度与实际温度的差异提示(「体感 32°,比实际热 4°,注意补水」)
- 降水概率(数据已有)
- 定位自动填充(现在要手填经纬度,或从 8 个预设城市里选,这很反人类)

### 3.2 同类「后端有数据、前端没画完」的工具

审计下来还有这几个:

| 工具 | 缺什么 |
|---|---|
| IP 查询 | 返回了 ISP、时区、经纬度,只显示了国家城市 |
| 网站体检 | 返回了分段耗时和安全头评分,只显示了状态码 |
| SSL 证书 | 返回了完整证书链,只显示了到期时间 |
| 汇率 | 返回了 160 种货币,UI 只放了 8 个 |
| 节假日 | 返回了全年数据,只显示了下一个假期 |

### 3.3 空状态和错误文案

- 蓝牙扫描空列表 → 现在一片空白,应该说「没搜到设备。确认蓝牙已开、目标设备在配对模式」
- 网络错误 → 现在弹 `IOException`,应该说「网络不通,检查一下 WiFi」
- 权限拒绝 → 现在灰着按钮不解释,应该说「需要相机权限才能扫码,去设置里打开」

---

## Part 4. 构建与发布(本地一律不跑)

本地 PRoot 起不了 AAPT2 daemon,这个环境构建不了 APK,以后不在本地试。

**已建 `.github/workflows/android.yml`**:push 到 main 自动构建 debug + release 双产物,传到 Artifacts。

补充要做的:
- Codespaces 的 `devcontainer.json`(需要开模拟器实机点测时用)
- release 签名:现在用 debug 签名,升级安装要卸载重装。需要建 keystore 并配 GitHub Secrets

---

## 排期建议

### 第一批(先修坏的,2 天)
1. **底栏重写** —— 消除 API < 33 的选中态失效 ★最优先
2. 主页数据刷新(20 行)
3. 天气定位自动填充 + 逐时曲线
4. 空状态与错误文案批量整改

### 第二批(预测大脑,4 天)
5. `UsageEvent` 采集层 + SQLite 表
6. `ToolScorer` 打分引擎 + 冷启动先验表
7. 主页「此刻」区块 + 推荐理由文案
8. 长按降权、设置页开关

### 第三批(主页看板,3 天)
9. 卡片就地操作(打勾 / 记账 / +1)
10. 卡片配置与排序
11. 全部工具默认折叠

### 第四批(补完,3 天)
12. 5 个「数据没画完」的工具补全
13. release 签名 + 正式发版

---

## 需要你拍板的三件事

1. **底栏**:按上面的方案重写(保留玻璃、砍掉障眼法),还是你想保留三层折射效果、只加 API 判断降级?
2. **「此刻」露几个**:我倾向 2 个(克制,不喧宾夺主),也可以 4 个。
3. **主页全部工具默认折叠**吗?折叠后首屏干净,但找工具多一次点击。

---

*方案版本:v2 · 2026-07-25*
