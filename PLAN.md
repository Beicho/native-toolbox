# Astro Kit 改造方案 v3

> **一句话定位**:一个会读心的工具箱。你想用什么,它已经放在手边了。
>
> 预测不是功能之一,它**是产品本身**。其他一切 —— 主页、小组件、数据层 —— 都是为它服务的输入源和输出口。

---

## Part 1. 预测大脑(产品核心)

### 1.1 目标体验:让用户以为被监听了

用户该有的反应:「**这怎么知道的?**」

具体场景:

| 情境 | App 的表现 | 用户感受 |
|---|---|---|
| 每天早上 7:40 挤地铁时打开 | 顶部直接是「通勤」组:番茄钟、白噪音、公交码 | 它知道我在路上 |
| 刚从相册分享一张图进来 | 顶部变成:压缩、抠图、加水印、OCR | 它知道我要处理这张图 |
| 周五晚上 8 点打开 | 顶部是:记账、待办结算、下周倒数日 | 它知道我要收尾一周 |
| 插上充电器 + WiFi | 悄悄把「视频压缩」「批量转换」这些耗电重活提上来 | 它知道现在适合干重活 |
| 复制了一串英文 | 顶部第一个就是翻译,且已经预填了剪贴板内容 | **它是不是看了我剪贴板?** |
| 连上车载蓝牙 | 顶部:导航去找车、行车记录、油耗计算 | 它知道我上车了 |
| 到了一个陌生 WiFi(酒店) | 顶部:WiFi 二维码、网速测试、临时邮箱 | 它知道我在出差 |
| 手机剩 15% 电 | 耗电工具全部下沉,「省电模式」「手电筒」提上来 | 它知道我电量焦虑 |

**关键:这些全部纯本地推断,零网络,零 LLM。** 隐私是卖点,不是妥协。

### 1.2 信号采集(比 v2 大幅扩展)

之前只想到时间和使用记录。真正能做出"读心"效果的,是**环境信号的组合**:

#### A. 时间维度
- 精确到 15 分钟的时段桶(不是整小时 —— 7:45 和 7:15 的行为差别很大)
- 星期几 / 是否周末 / 是否节假日(节假日表本地已有)
- 距离上次使用间隔(刚用完 vs 隔了三天,意图完全不同)

#### B. 设备状态(全部免权限)
- 充电中 / 电量档位(<20% / 20-50% / >50%)
- 网络类型:WiFi(记 SSID 哈希,不存明文)/ 蜂窝 / 断网
- 蓝牙已连接设备类型(耳机 / 车载 / 手表)—— 车载蓝牙是"我在开车"的强信号
- 屏幕方向(横屏 → 可能在看视频/玩游戏)
- 音频输出通道(扬声器 / 耳机)
- 是否刚解锁(冷启动 vs 后台切回)

#### C. 意图信号(最强,但要小心)
- **剪贴板类型嗅探**:只判断"是不是 URL / 是不是纯英文 / 是不是一串数字",**绝不读取内容本身,绝不上传**。Android 10+ 只有前台能读,天然受限
- **分享进入**:ACTION_SEND 带来的 MIME 类型(image/* → 图片工具,text/* → 文本工具)
- 上一个用的工具(一阶马尔可夫)
- 上两个用的工具(二阶,识别"链路":拍照→OCR→翻译)

#### D. 使用质量
- 停留时长(<3 秒 = 误触,负权重)
- 是否产生了输出(存了文件/复制了结果 = 强正权重)
- 是否从推荐位点进去的(用于评估推荐质量,闭环)

### 1.3 打分引擎

```
score(tool) = Σ wᵢ · featureᵢ  −  penalties
```

七项特征,全是查表和乘加,一次算 200 个工具 < 5ms:

| 特征 | 权重 | 说明 |
|---|---|---|
| 近期热度 | 0.20 | 指数衰减,半衰期 14 天 |
| 时段亲和 | 0.25 | 当前 15 分钟桶 ± 1 的历史命中率 |
| 连带概率 | 0.20 | 一阶 + 二阶马尔可夫转移 |
| 情境匹配 | 0.25 | 设备状态组合命中(充电/网络/蓝牙/电量) |
| 意图匹配 | 0.30 | 剪贴板类型 / 分享 MIME(命中时权重最高) |
| 长期偏好 | 0.10 | 总使用占比(避免完全被短期波动带跑) |
| 新鲜度奖励 | 0.05 | 从没用过的工具偶尔冒泡一次(避免信息茧房) |

**惩罚项**:
- 长按「不要推荐这个」→ ×0.15,衰减 30 天
- 连续 3 次出现在推荐位但没被点 → ×0.6(它猜错了,自己认账)
- 断网时联网工具 → ×0.1(不推荐一个点开就报错的东西)

### 1.4 情境模式识别(让"读心感"成立的关键)

单个信号没用,**组合才有意义**。内置若干情境模式,匹配到就整组推荐:

| 模式 | 触发条件 | 推荐组 |
|---|---|---|
| 通勤 | 工作日 7-9 或 18-20 点 + 蜂窝网络 + 移动中 | 番茄钟、白噪音、待办 |
| 开车 | 车载蓝牙已连 | 挪车码、油耗、导航找车、分贝仪 |
| 出差 | 陌生 WiFi + 非常用时段 | WiFi 码、网速、临时邮箱、汇率、时区 |
| 处理图片 | 从相册分享进入 or 剪贴板是图片 | 压缩、抠图、水印、OCR、格式转换 |
| 办公 | 工作日 9-18 + WiFi + 横屏 | PDF、OCR、翻译、文件传输、二维码 |
| 睡前 | 22 点后 + 电量 > 30% | 白噪音、屏幕时间、闹钟、护眼 |
| 充电闲置 | 充电中 + WiFi + 屏幕刚亮 | 视频压缩、批量转换、备份(耗时重活) |
| 低电量 | < 20% 且未充电 | 省电、手电筒(轻量);耗电工具全下沉 |

情境模式的权重占 `情境匹配` 那 0.25,不会完全盖过个人习惯。

### 1.5 冷启动与过渡

前两周没有个人数据,靠**通用先验表**(按时段 + 情境),同时:

```
最终分 = α · 先验 + (1−α) · 个人分
α: 1.0 → 0,30 天线性衰减
```

**前 3 天不显示「此刻」区块**,先默默收集。第 4 天开始出现,此时至少有几十条记录,推荐不会太蠢。第一次出现时给一句轻量说明:「我开始学你的习惯了,常用的会自动放到这里」。

### 1.6 呈现:克制到极致

**只在主页顶部一行,2-3 个卡片,不占满一屏。**

```
┌────────────────────────────┐
│  早上好                     │
│                            │
│  此刻                       │
│  ┌────────┐┌────────┐      │
│  │📷 压缩  ││✂️ 抠图 │      │
│  │刚分享的 ││        │      │
│  │那张图   ││        │      │
│  └────────┘└────────┘      │
└────────────────────────────┘
```

每个卡片下面**一行小字说明理由**:「刚分享的那张图」/「你常在早上用」/「刚复制了英文」。

理由文案是"读心感"的一半 —— 用户看到理由才会惊讶,而不是觉得随机。同时这也是透明度:他知道 App 在看什么,而不是黑盒。

**长按 → 「不要再推荐这个」/「看看它为什么在这」**。可纠正、可解释,信任才建立得起来。

### 1.7 隐私必须说清楚(这是产品叙事的一部分)

设置页要有一个显眼的「预测说明」页:

- 所有分析**只在你手机里**,一个字节都不上传
- 剪贴板只判断类型(网址/英文/数字),**不读内容、不存内容**
- WiFi 只存名字的哈希值,认得出"是不是同一个网",但还原不出叫什么
- 完全不碰:位置、通讯录、通话记录、其他 App 的数据
- 一键「清空学习记录」/「关闭预测」

**"它好像监听了我,但它其实什么都没上传"** —— 这个反差本身就是最好的宣传。

### 1.8 落地拆解

| 模块 | 文件 | 说明 |
|---|---|---|
| 事件表 | `data/predict/UsageEvent.kt` + Room DAO | 单表,带索引 |
| 信号采集 | `data/predict/SignalCollector.kt` | 设备状态嗅探,全部免权限 |
| 情境识别 | `data/predict/ContextMatcher.kt` | 8 个模式的规则匹配 |
| 打分引擎 | `data/predict/ToolScorer.kt` | 七特征加权 |
| 先验表 | `data/predict/ColdStartPriors.kt` | 时段 × 情境默认推荐 |
| 理由生成 | `data/predict/ReasonText.kt` | 把命中的特征翻成人话 |
| UI | `ui/home/NowSection.kt` | 主页顶部区块 |

---

## Part 2. 数据层重建(预测的地基)

**这是预测的前提,不是独立任务。** 现在的数据层根本承不住预测:

### 2.1 现状有多糟

记账数据的真实存储:**一坨空格分隔的文本行**塞在 SharedPreferences 的 `raw` 字段:

```
2026-07-25 38 餐饮 午饭外卖
```

解析靠 `split(Regex("[\\s,，]+"))`。后果:
- **备注打空格就串行**,分类被吃掉
- 没有 ID,改一条要重写整个字符串
- **9 个工具 9 个独立 prefs,格式互不相同**
- **没有导出**,换手机全丢

### 2.2 为什么预测必须先修这个

预测引擎要读「用户的记账频率」「待办完成率」「倒数日临近度」来做情境判断。数据是文本行的话,预测引擎得写 9 套解析器,而且随时被脏数据搞崩。

**统一 Room + 统一 schema,预测引擎才有干净的输入。**

### 2.3 要做的

- Room 数据库,每类数据一张表,统一带 `id` / `createdAt` / `updatedAt` / `deletedAt`(软删除)
- 从旧 prefs 一次性迁移(读旧文本 → 尽力解析 → 写入表 → 标记已迁移)
- **一键导出全部数据为 JSON**,一键导入。这是用户敢长期用的前提
- 预测引擎通过 DAO 读聚合结果,不碰原始文本

---

## Part 3. 出口扩展(预测的价值放大器)

预测算得再准,锁在 App 里也白搭。**要让它出现在用户看得见的地方。**

### 3.1 桌面小组件(Glance)—— 最重要的出口

现在项目里 `AppWidgetProvider` 一个都没有。

**「此刻」小组件**:桌面上一个 2×2 或 4×2 的格子,直接显示预测出的 2-4 个工具图标。用户解锁手机就看见,点一下直达。

这是**比主页更强的存在感** —— 用户根本不用打开 App,预测结果已经在桌面上了。而且这是 Android 独占优势,iOS 抄不走。

还要做:倒数日、待办清单、天气、习惯打卡进度。

### 3.2 快捷设置磁贴(TileService)

下拉通知栏就能点:手电筒、白噪音、屏幕常亮、快速记账。一天点两次,存在感就有了。

### 3.3 系统全局搜索(AppSearch / App Actions)

用户在系统搜索框打"二维码",应该能直接跳进扫码工具。**免费流量入口**,现在完全没接。

---

## Part 4. 包体瘦身(100MB → 目标 30MB 内)

拆了依赖缓存,真凶找到了 —— 不是我之前以为的本地词库:

| 依赖 | 体积 | 谁在用 |
|---|---|---|
| `mlkit:mediapipe-internal` | **22 MB** | 人像分割(抠图/证件照) |
| `mlkit:text-recognition-bundled-common` | **18 MB** | OCR 中文词库 |
| `mlkit:text-recognition-chinese` | 2 MB | OCR |
| `assets/phone.dat` | 4.3 MB | 手机号归属地 |
| `assets/pinyin.tsv` | 288 KB | 拼音标注 |
| **小计** | **~47 MB** | **5 个工具** |

**5 个工具吃掉一半包体,所有用户都得下。**

### 解决方案

1. **OCR / 抠图改用 Play 服务动态下发版**
   - `text-recognition-chinese` → `play-services-mlkit-text-recognition-chinese`
   - `segmentation-selfie` → 同理
   - 模型改为首次使用时从 Play 服务按需下载
   - **省 ~40 MB**,代价:首次用需要联网下模型(给明确提示)
   - 无 Play 服务的机器降级提示"此功能需要 Google 服务"

2. **词库改服务端下发**
   - phone.dat / pinyin.tsv 首次使用时从自己的 PHP 后端拉,存 filesDir
   - **省 4.6 MB**

3. **开 ABI 分包**
   - 现在一个 APK 包含所有架构的 native 库
   - 按 arm64-v8a / armeabi-v7a 分开出包,或直接只发 arm64(2026 年了)
   - **省 30-40%**

4. **确认 R8 真的在跑**
   - 检查 release 是否真的 minify + resource shrink

**目标:主包 25-30 MB。**

---

## Part 5. 修坏的

### 5.1 已修

**剪贴板暂存架 / 文本模板点开闪退** —— 是我昨天加持久化时埋的雷:
`rememberPrefString` 用 `LaunchedEffect(state.value)` 落盘,一次交互连改两个持久化状态(`add()` 同时改 `shelf` 和 `input`)会反复重启协程,在 LazyColumn 重组期写状态 → 无限重组 → 闪退。这两个恰好是唯一在单次交互里连改两个持久化状态的,所以只有它们崩。已改成 setter 内同步写盘。

### 5.2 底栏液态玻璃(拆了 AAR 字节码,找到真因)

`PlatformKt` 暴露两个**独立**开关:

```java
isRenderEffectSupported()   // API 31+
isRuntimeShaderSupported()  // API 33+  ← lens 折射真正依赖这个
```

代码只判断了前者。而底栏选中态是"三层障眼法":第二层是 `alpha(0f)` 的强调色图标,**全靠第三层滑块的 `lens` 折射把它透出来**。

所以 **API < 33 的机器上 `lens` 是空操作,强调色图标永远透不出来,用户完全看不出选中的是哪个 Tab** —— 只剩一块 alpha 0.1 的灰影。它在新机器上好看,在老机器上是坏的,而且坏得很安静。

另外两个:
- `vibrancy()` 属于 `ColorFilterKt`(颜色滤镜),不是玻璃质感,放 blur 前会让深色模式发灰
- 滑块盖住当前 Tab **吞掉了点击**,点当前页没反应

**修法**:253 行砍到 ~70 行。选中态改成直接给图标上色(不依赖任何 shader,所有机器都对),玻璃面板保留,`lens` 只在 API 33+ 时叠加当锦上添花,删掉第二层和拖拽手势。

### 5.3 正式签名(发版前置条件)

`signingConfig = signingConfigs.getByName("debug")` —— release 用 debug 签名。

后果:**每次升级必须先卸载,卸载就清数据**。刚教会用户记账、设了倒数日,一升级全没了。和数据层问题叠加是致命的。而且 debug 签名任何人都能重签伪装。

必须建 keystore + GitHub Secrets。

### 5.4 其他

- 主页数据从工具页返回不刷新(`LaunchedEffect(Unit)` 只跑一次)
- 天气要手填经纬度(该自动定位)
- 5 个工具「后端返回了数据、前端只画了一部分」:IP 查询、网站体检、SSL 证书、汇率、节假日
- 空状态一片空白、错误直接抛 `IOException`

---

## 排期

| 批次 | 内容 | 天 | 为什么这个顺序 |
|---|---|---|---|
| **0** | 闪退修复(已完成)、底栏重写、正式签名 | 1.5 | 现在就是坏的 |
| **1** | Room 数据层 + 迁移 + 导出导入 | 2 | **预测的地基**,不修预测没有干净输入 |
| **2** | **预测大脑**:采集 → 情境 → 打分 → 理由 | 4 | 核心 |
| **3** | 主页「此刻」+ 卡片就地操作 | 2 | 预测的第一出口 |
| **4** | **桌面小组件「此刻」** + 快捷磁贴 | 2.5 | 预测的最强出口 |
| **5** | 包体瘦身(ML Kit 动态下发 + ABI 分包) | 1.5 | 100MB 是安装转化率杀手 |
| **6** | 补完:5 个数据没画完的工具、空状态、错误文案、系统搜索 | 2 | 长尾 |

**合计 ~15.5 天。** 第 1-2 批是主线,其他都在给它铺路或放大它。

---

## 需要你拍板

1. **底栏**:按方案重写(砍障眼法、选中态直接上色),还是保留三层折射只加 API 降级?
2. **剪贴板嗅探**:这是"读心感"最强的信号,但也最敏感。做(只判类型不读内容)还是完全不碰?
3. **ML Kit 改动态下发**:省 40MB,代价是 OCR/抠图首次用要联网下模型,且依赖 Google 服务。换还是不换?
4. **「此刻」露几个**:我倾向 2-3 个。

---

*v3 · 2026-07-25*

---

# 执行记录(2026-07-26 凌晨,一夜完成)

## 已完成 ✅

| 批次 | 内容 | 提交 |
|---|---|---|
| 0 | 底栏重写(API<33 选中态失效修复,253→150 行) | 构建绿 |
| 0 | 剪贴板暂存架/文本模板闪退(Persist.kt 组合期写状态) | 构建绿 |
| 1 | AstroStore 结构化存储(不用 Room,零 KSP 风险)+ LegacyMigration + 导出导入 | 构建绿 |
| 2 | **预测大脑**:SignalCollector(15min桶/充电/网络/蓝牙类别/剪贴板形状嗅探/分享MIME)+ ContextMatcher 15 情境 + PredictEngine 七特征打分 + 理由生成 | 构建绿 |
| 3 | 主页「此刻」(带理由/长按拒绝/不足8条不显示)+ 卡片就地操作(待办打勾/记账直接记/倒数日点切) | 构建绿 |
| 4 | 桌面小组件 NowWidget(Glance)+ 4 个快捷磁贴(手电筒磁贴上直接开关) | 构建绿 |
| 5 | 包体瘦身:词库走 39.98.89.19 下发(AssetGate 引导)、抠图换自研 SmartCutout(-22MB mediapipe)、ABI 分包、OCR 保留(刚需) | 构建绿 |
| 6 | 正式签名(4096 RSA,GitHub Secrets)、预测隐私设置页、数据备份恢复 | 构建绿 |
| 补 | SSL/网站体检字段名读错全空(chain[0]/timing/security.detail)、IP 安全标记、汇率 160 币种 | 构建绿 |
| 补 | 倒数日/记账/待办便签三个核心工具全面重做(AstroStore 三处同源) | 构建绿 |
| 补 | 全局卡片间距、网格卡固定两行、Pager 固定高度 | 构建绿 |
| 补 | 拼音首字母搜索(jsq→计算器,GB2312 分界表法,JVM 验证) | 待验证 |
| 测 | emutest 升级为全量 200 路由遍历(之前只测 23 个) | 跑批中 |

## 还欠 ⏳

- 全量模拟器测试结果(200 路由遍历,跑批中)
- release.yml 发版流程用新签名出正式版
- 空状态批量补完(蓝牙已做,WiFi 分析等长尾)
- 系统全局搜索(AppSearch)
